# ftp://ftp.osuosl.org/pub/slackware/${SLACKWARE}-${DISTRO_COMPAT_VERSION}/ChangeLog.txt
#
# must source DISTRO_SPECS first..
#
# mirrors: http://salixos.org/mirmon.html
#

if [ "$DISTRO_COMPAT_VERSION" = "" ] ; then
	[ -f ./DISTRO_SPECS ] && . ./DISTRO_SPECS
fi

case "$DISTRO_TARGETARCH" in
	x86)
		SLACKWARE=slackware
		SLACKARCH=i486
		;;
	x86_64)
		SLACKWARE=slackware64
		SLACKARCH=x86_64
		;;
esac

#----------------------
#PKG_DOCS_DISTRO_COMPAT - where to download the compat-distro pkgs databases from
#---------------------
# 1|2|3
#   1 - domain. for testing the url.
#   2 - full URI of the database file.
#   3 - name of db file when local and after being processed into standard format

PKG_DOCS_DISTRO_COMPAT="
z|ftp://ftp.nluug.nl/pub/os/Linux/distr/salix/${SLACKARCH}/slackware-${DISTRO_COMPAT_VERSION}/PACKAGES.TXT|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-official
z|ftp://ftp.nluug.nl/pub/os/Linux/distr/salix/${SLACKARCH}/${DISTRO_COMPAT_VERSION}/PACKAGES.TXT|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-salix
z|http://repository.slacky.eu/${SLACKWARE}-${DISTRO_COMPAT_VERSION}/PACKAGES.TXT|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-slacky
"

#-------------------
#REPOS_DISTRO_COMPAT - hardcode the compat-distro repos in here...
#-------------------
# 1|2|3
#   1 - domain. for testing the url.
#   2 - full URI of the repo
#   3 - name of db-file(s) associated with that repo. it may have glob wildcards.

REPOS_DISTRO_COMPAT="
z|ftp://ftp.nluug.nl/pub/os/Linux/distr/salix/${SLACKARCH}/slackware-${DISTRO_COMPAT_VERSION}|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-official
z|ftp://ftp.nluug.nl/pub/os/Linux/distr/salix/${SLACKARCH}/${DISTRO_COMPAT_VERSION}|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-salix
z|http://slackware.uk/salix/${SLACKARCH}/slackware-${DISTRO_COMPAT_VERSION}|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-official
z|http://slackware.uk/salix/${SLACKARCH}/${DISTRO_COMPAT_VERSION}|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-salix
z|ftp://ftp.gtlib.gatech.edu/pub/salixos/${SLACKARCH}/slackware-${DISTRO_COMPAT_VERSION}|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-official
z|ftp://ftp.gtlib.gatech.edu/pub/salixos/${SLACKARCH}/${DISTRO_COMPAT_VERSION}|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-salix
z|http://mirror.aarnet.edu.au/pub/slackware/${SLACKWARE}-${DISTRO_COMPAT_VERSION}|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-official
z|http://slackware.cs.utah.edu/pub/slackware/${SLACKWARE}-${DISTRO_COMPAT_VERSION}|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-official
z|http://slackware.mirrors.tds.net/pub/slackware/${SLACKWARE}-${DISTRO_COMPAT_VERSION}|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-official
z|http://ftp.gwdg.de/pub/linux/slackware/${SLACKWARE}-${DISTRO_COMPAT_VERSION}|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-official
z|http://ftp.isu.edu.tw/pub/Linux/Slackware/${SLACKWARE}-${DISTRO_COMPAT_VERSION}|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-official
z|http://repository.slacky.eu/${SLACKWARE}-${DISTRO_COMPAT_VERSION}|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-slacky
z|http://slackware.uk/${SLACKWARE}-${DISTRO_COMPAT_VERSION}|Packages-${SLACKWARE}-${DISTRO_COMPAT_VERSION}-slacky
"


#---------------
# fix variables
#---------------
PKG_DOCS_DISTRO_COMPAT="$(echo "$PKG_DOCS_DISTRO_COMPAT" | sed '/^$/d' | tr '\n' ' ' | sed 's% $%%')"
REPOS_DISTRO_COMPAT="$(echo "$REPOS_DISTRO_COMPAT" | sed '/^$/d' | tr '\n' ' ' | sed 's% $%%')"

