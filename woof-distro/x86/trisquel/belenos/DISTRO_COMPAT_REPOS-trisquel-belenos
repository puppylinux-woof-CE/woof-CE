#where to get pkg docs, format: domain|uri|localfilename...
PKG_DOCS_DISTRO_COMPAT="us.archive.trisquel.info|http://us.archive.trisquel.info/trisquel/dists/${DISTRO_COMPAT_VERSION}/main/binary-i386/Packages.bz2|Packages-${DISTRO_BINARY_COMPAT}-${DISTRO_COMPAT_VERSION}-main"
#ubuntu repos...
#note, the local pkgs database file (or partname) is also appended to these entries, so know which url to download a particular pkg from (glob wildcard allowed)...
REPOS_DISTRO_COMPAT="us.archive.trisquel.info|http://us.archive.trisquel.info/trisquel|Packages-${DISTRO_BINARY_COMPAT}-${DISTRO_COMPAT_VERSION}-*"
