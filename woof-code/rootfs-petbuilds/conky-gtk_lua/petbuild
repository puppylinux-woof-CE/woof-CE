# leave petbuild empty since we already have the whole source
# this is created as a petbuild instead of package so that the it can be manually endabled/disabled in _00build.conf

download() {
    echo -n
}

build() {
    echo -n
}
