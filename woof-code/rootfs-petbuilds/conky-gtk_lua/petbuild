download() {
    # leave petbuild download empty since we already have the whole source
    echo -n
}

build() {
    # enable conky autostart
    # rest of the setup is handled by conky-gtk on its first run
    mkdir -p /root/Startup/
    cp ./root/.config/conky-gtk/startup_script_format /root/Startup/conkystart
}
