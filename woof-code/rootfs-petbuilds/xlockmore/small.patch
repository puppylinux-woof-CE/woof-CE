diff -rup xlockmore-5.66-orig/xlock/mode.h xlockmore-5.66/xlock/mode.h
--- xlockmore-5.66-orig/xlock/mode.h	2020-09-23 03:23:27.000000000 +0300
+++ xlockmore-5.66/xlock/mode.h	2020-12-28 18:24:27.490532081 +0200
@@ -328,6 +328,14 @@ extern void release_last_mode(ModeInfo *
 
 /* -------------------------------------------------------------------- */
 
+/*BK...*/
+#define MODE_matrix
+#define MODE_lyapunov
+#define MODE_goop
+#define MODE_dclock
+#define MODE_ball
+
+#if 0
 /* if you just want the blank mode... how sad! */
 #ifndef BLANK_ONLY
 #ifndef NICE_ONLY
@@ -590,6 +598,7 @@ extern void release_last_mode(ModeInfo *
 
 #endif /* NICE_ONLY */
 #endif /* BLANK_ONLY */
+#endif
 
 #ifndef USE_MODULES
 
