#!/bin/sh

if [ -f usr/share/applications/galculator.desktop ] ; then
	sed -i \
		-e 's%Name=.*%Name=Galculator scientific calculator%' \
		-e 's%Categories=.*%Categories=Calculator%' \
			usr/share/applications/galculator.desktop
fi
