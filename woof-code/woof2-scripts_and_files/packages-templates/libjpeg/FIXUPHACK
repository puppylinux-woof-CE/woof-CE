
#no, ttuuxxx reports the old lib not compatible, so i am also installing libjpeg_old-6b...
#if [ -e usr/lib/libjpeg.so.7 ];then
# if [ ! -e usr/lib/libjpeg.so.62 ];then
#  ln -s libjpeg.so.7 usr/lib/libjpeg.so.62
# fi
#fi

#mageia 1 has libjpeg 8, but some pkgs need libjpeg7...
#mageia, note, have also installed libjpeg6.
#NO, symlink does not work, instead using libjpeg_old7 pet (in common repo)...
#JPEGLIB="`find usr/lib -type f -name 'libjpeg.so.*' | head -n 1`"
#if [ "$JPEGLIB" ];then
# BASEJPEGLIB="`basename $JPEGLIB`"
# if [ ! -e usr/lib/libjpeg.so.7 ];then
#  ln -s ${BASEJPEGLIB} usr/lib/libjpeg.so.7
# fi
#fi
