USAGE="Usage: ${bold}pkg bleeding-edge [yes|no]${endcolour}

If yes, always re-sort package lists to put newest package 
versions before older packages.

If no, packages will be listed in the repo fall back order, 
which is defined in $PKGRC.

Give no yes|no option to see current setting"
