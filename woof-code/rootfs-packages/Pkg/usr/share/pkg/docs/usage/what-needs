USAGE="Usage: ${bold}pkg what-needs PKGNAME${endcolour}

List the packages that depend on PKGNAME.

PKGNAME must be an installed package.

Use ${bold}--force${endcolour} to list all dependencies,
including built-in and devx packages."
