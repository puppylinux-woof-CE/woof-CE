USAGE="Usage: ${bold}pkg list-deps PKGNAME${endcolour}

List the dependencies of PKGNAME
By default only missing dependencies are listed.

PKGNAME must be a valid repo package.

Use ${bold}--force${endcolour} to list all dependencies,
including installed and builtin packages."
