. /root/.pkg/pkgrc
USAGE="Usage: ${bold}pkg names-exact PKGNAME${endcolour}

Search the current repo ($REPONAME) for 
matching package names, exact match.

PKGNAME is an optional filter.

Support environment variables:

HIDE_INSTALLED=true|false #hide installed pkgs from search or not"
