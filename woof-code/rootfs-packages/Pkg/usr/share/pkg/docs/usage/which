USAGE="Usage: ${bold}pkg which FILE${endcolour}

Print name of package containing FILE.

FILE can be the name of a command in \$PATH,
or the full path to a regular file.

For best results, FILE should be the 
full path to the file or command."
