USAGE="Usage: ${bold}PKGRC=/path/to/file${endcolour} pkg <options>

Use a different rc file (config file) than the default, 
which is currently ${bold}$PKGRC${endcolour}.

You can see the current settings with:
  ${bold}$SELF show-config${endcolour}"
