USAGE="Usage: ${bold}pkg update-sources PKGNAME${endcolour}

Refresh the list of available repos on your system.

You must do this if you add/remove any repos, 
or make any changes to these files:

${bold}  /root/.pkg/sources
  /root/.pkg/sources-all${endcolour}"
