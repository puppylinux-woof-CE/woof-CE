USAGE="Usage: ${bold}pkg split PKGFILE${endcolour}

Split a single package file into DEV, DOC, NLS packages. The newly
created files will have the same filetype as the original package
(.pet,.deb, .txz, .sfs, etc).

PKGFILE should be a valid local package file.
"
