USAGE="Usage: ${bold}pkg repack PKGNAME${endcolour}

Re-package an already installed package.
PKGNAME must be an installed package.

Packages are created from the file list in:
~/.packages/PKGNAME.files

You can add/remove file paths to those files 
before you re-package them."
