USAGE="Usage: ${bold}pkg func-list PKGNAME${endcolour}

Lists the internal functions that are available 
to use directly on the command line.

To use Pkg internal funcs, type in the console:
  source /usr/sbin/pkg

Many of Pkgs internal funcs will now be available 
on the command line.

This is feature EXPERIMENTAL.

List of funcs:
$(pkg func-list)
"
