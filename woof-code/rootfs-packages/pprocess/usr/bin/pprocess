#!/bin/ash

if [ "$DISPLAY" ] ; then
	if type lxtask >/dev/null 2>&1 ; then
		exec lxtask "$@"
	else
		exec /usr/lib/gtkdialog/box_ok "pprocesss" error "Please install lxtask ..."
	fi
else
	type htop >/dev/null 2>&1 && exec htop "$@"
	type top >/dev/null 2>&1 && exec top "$@"
fi
