jwm is a window manager and tray manager.

It is a binary executable, written in C. The executable is at /usr/bin.

/usr/share/doc/nls/jwm/jwm.pot is in the 'devx'.

MoManager, the Translation manager, reads the .pot file.
