#!/bin/sh

if [ "$DISPLAY" ] ; then
	which galculator &>/dev/null && exec galculator "$@"
	which kcalc &>/dev/null && exec kcalc "$@"
	which ycalc &>/dev/null && exec ycalc "$@"
	which xcalc &>/dev/null && exec xcalc "$@"
fi

