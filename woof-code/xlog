#!/bin/sh

if [ -f "$1" ] ; then
	sh "$1" 2>&1 | tee "${1}.log"
else
	echo "Usage:

xlog 0setup
xlog 1download
xlog 2createpackes
xlog 3builddistro-Z
...etc...

This creates 0setup.log, 1download.log, etc
"
fi

